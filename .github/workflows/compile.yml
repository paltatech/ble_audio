name: compile

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - develop

env:
  PROJECTS_PATH: zephyr-workspace/projects
  FIRMWARE_PATH: zephyr-workspace/projects/ble_audio
  NCS_VERSION: v2.7.0

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: ${{ env.FIRMWARE_PATH }}
          fetch-depth: 0

      - name: Install nrfutil toolchain manager
        run: |
          curl https://files.nordicsemi.com/artifactory/swtools/external/nrfutil/executables/x86_64-unknown-linux-gnu/nrfutil -o nrfutil
          chmod +x nrfutil
          ./nrfutil install toolchain-manager
          ./nrfutil toolchain-manager install --ncs-version ${{ env.NCS_VERSION }}
          rm /home/runner/ncs/toolchains/*/usr/local/bin/git || true

      # See test.yml for why this is one continuous script rather than
      # separate steps, and why it assumes a MY_GITHUB_TOKEN secret.
      - name: Init west workspace and build
        env:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
        run: |
          set -e

          NCS_TOOLCHAIN=$(ls -d "$HOME"/ncs/toolchains/*/ | head -1)
          NCS_TOOLCHAIN=${NCS_TOOLCHAIN%/}
          export NCS_TOOLCHAIN
          export PATH="$NCS_TOOLCHAIN/usr/local/bin:$NCS_TOOLCHAIN/usr/local/cmake:$PATH"
          export LD_LIBRARY_PATH="$NCS_TOOLCHAIN/usr/local/lib:$LD_LIBRARY_PATH"
          export ZEPHYR_TOOLCHAIN_VARIANT=zephyr
          export ZEPHYR_SDK_INSTALL_DIR="$NCS_TOOLCHAIN/opt/zephyr-sdk"

          cd ${{ env.PROJECTS_PATH }}
          git config --global url."https://x-access-token:${GITHUB_TOKEN}@github.com/paltatech/".insteadOf "git@github.com:paltatech/"

          west init -l ble_audio
          mv .west ../
          west config manifest.path projects/ble_audio
          west update --narrow -o=--depth=1

          pip install -r ../zephyr/scripts/requirements.txt
          pip install -r ../nrf/scripts/requirements.txt

          source ../zephyr/zephyr-env.sh
          west zephyr-export

          cd ble_audio
          LOG_FILE="build_output.log"
          set -o pipefail
          ./prepare_release.sh 2>&1 | tee "$LOG_FILE"

          WARNING_COUNT=$(awk '/: warning:/' "$LOG_FILE" | wc -l | xargs)
          echo "Build completed with $WARNING_COUNT warning(s)"

          if [ "$WARNING_COUNT" -gt 0 ]; then
            echo "FAILED - build has $WARNING_COUNT warning(s)"
            exit 1
          fi
          echo "PASSED - build completed with no warnings"

      - uses: actions/upload-artifact@v4
        with:
          name: ble_audio-firmware
          path: ${{ env.FIRMWARE_PATH }}/release/*
          if-no-files-found: error
